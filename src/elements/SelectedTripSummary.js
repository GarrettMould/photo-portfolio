import { StyledSelectedTripSummary } from "./styles/SelectedTripDetails.styled";

const SelectedTripSummary = (props) => { 
    return <div>
    <SelectedTripSummary>This trip included an eventful three-day vacation to Nha Trang, as well as my first trip overseas since before the pandemic. I saw a bit of everything, from beaches, to waterfalls, to the impressive Singaporean city scape.</SelectedTripSummary>
    </div>
}

export default SelectedTripSummary;