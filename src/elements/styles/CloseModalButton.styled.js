import styled from "styled-components";

export const CloseModalContainer = styled.div`
    z-index: 999;
    position: fixed; 
    top: 10px; 
    right: 10px;

    :hover { 
        cursor: pointer;
    }
`